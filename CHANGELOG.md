# 更新日志（Changelog）

## v0.2.1 (2026-08-23)

设置面板全面重排 + 一键迁移 API Key，并合并自 v0.2.0 发布以来的默认行为变更与检测增强。

### 新功能

- **设置面板重排**：六个编号分区（① 引擎与密钥 → ⑥ 个人词库与系统提示词），Base URL 与「测试连接」上移到引擎区、多引擎路由与长文强模型从折叠层提升为独立分组、术语表与系统提示词成组——常用项不再深藏折叠层。
- **剪贴板快速迁移**：设置页新增「复制全部设置（含 Key）/ 从剪贴板导入」一对按钮，同一浏览器内换装新版本时一键带走全部配置；未配置任何 Key 时自动显示迁移引导提示。

#### 行为与修复（自 v0.2.0 发布以来）

- **默认翻译模式改为「手动点击·划词」**：进入网页不再自动整页翻译，由用户点击段落 / 划词 / 点工具栏按需触发。内置一次性迁移，老用户升级后同样切换（可在设置改回「自动」）。站点级「总是自动翻译此站」仍可覆盖全局手动模式，二者可叠加使用。
- **中文识别增强，杜绝中译中**：检测规则重写——拉丁占比 ≥0.55 才判英文；否则只要含汉字即判中文。覆盖「设置」「确定」等两字 UI 词与「这款 iPhone 15 Pro 值得买吗」等混排句，不再重复送翻；纯英文、日/韩/西里尔等判定不受影响。
- **译文落点智能回退**：插入后渲染帧内做几何校验（应位于锚点正下方、未跨列、未被裁剪），错位自动降级到备用落点；多列祖先扫描从 3 层加深到 6 层，flex/grid/float/绝对定位/table 系父容器全部纳入强制内嵌判定——长文排版下译文不再漂移到别的列或被裁剪。
- **工具栏模式标签竞态修复**：挂载早于配置读取时补一次回读，手动模式下悬停提示不再短暂显示为「翻译本页」。
- 官网与 README 同步以上行为描述；测试规模徽标改为通用表述（单测 63→66，总计 103 项全绿）。

## v0.2.0 (2026-08-23)

第一个功能版本：在 v0.1.x 的稳定性基线上新增四项高频价值功能，并修复一批并发与生命周期缺陷。

### 新功能

- **🔊 朗读译文（TTS）**：划词面板、悬停气泡、输入框结果三处均可一键朗读，
  按目标语言自动匹配发音；点击切换「⏹ 停止」，自然结束自动复位。
  基于浏览器内置语音合成，零依赖、离线可用。不支持 TTS 的环境按钮自动禁用。
- **翻译历史**（弹窗新「历史」标签页）：记录用户主动发起的单条翻译（划词 / 输入框 / 弹窗），
  支持关键词搜索、点击回填到文本翻译、一键清空；本地存储上限 200 条、同原文+译文去重置顶。
  悬停翻译刻意不记录（被动触发会产生噪声）。
- **缓存管理**（设置页）：实时显示翻译缓存条数，一键清空——排查"为什么命中了旧译文"
  或释放存储不再需要刷新页面清数据。
- **界面主题手动覆盖**：跟随系统之外可强制浅色 / 深色，悬浮工具栏、全部设置面板
  与结果浮层的深浅色统一生效。

### 缺陷修复（本轮专项审查发现）

- 快速设置面板快速双击齿轮会并发打开两次，先完成的面板失去引用成为**孤儿残留屏幕**
  （只能刷新页面消除）→ 打开流程加代际守卫，过期构建结果直接丢弃。
- 完整设置面板的 storage 监听从未退订：每次打开都净增一个 onChanged 监听 +
  一份被闭包持有的表单 DOM → dispose 时真正退订；「关闭→重开」竞态下旧异步构建
  覆盖新面板 formApi 的问题一并修复（构建加代际守卫，关闭时使在途构建失效）。
- 切换引擎时未保存的 API Key 提交失败会被静默吞掉且永不重试 → 失败归还脏标记并明确提示；
  表单保存的整个「读最新值→叠加→写回」改入串行队列，消除飞行中读取拿到旧基底
  导致覆盖并发写入的字段丢失窗口。
- 会话缓存不携带「已是目标语言」标志：跳过提示第二次起消失 → 跳过结果不再写入缓存，
  让本地判定每次重新给出提示。
- 手动模式点段落翻译不计入 translatedCount：工具栏 title 显示「翻译本页」但点击实际收起，
  行为与文案相反 → 与其它路径对齐计数并刷新。
- 流式端口绕过文本长度/空值校验（HTTP 通道有 20000 字符上限，端口没有）→ 统一校验；
  端口断连竞态下的 postMessage 异常不再产生未处理拒绝噪音。
- 弹窗文本翻译命中目标语言跳过时同样显示说明（与三个页面内浮层一致）。

### 体验优化

- 状态条宽度 240→300px 并支持两行换行；提示时长可按文案调整。

## v0.1.24 (2026-08-23)

三路深度审查（翻译核心 / 内容脚本 / UI 层）后的稳定性与正确性修复批次，
以及面板同步体系与设置备份功能。

### 面板同步与操作一致性

- 四个设置入口（页内快速面板、页内完整面板、Popup、Options）全部订阅同一份
  storage 配置：任一面板修改后其它面板实时刷新，无需手工转发。
- 完整表单保存改为**字段级脏跟踪**的读-改-写：以存储最新配置为基底，只叠加用户
  实际改过的字段——四个入口并发编辑不再互相覆盖丢字段；写失败自动归还待保存标记。
- 未保存的本地修改不被外部同步覆盖（fill 跳过 dirty 字段），保存成功后恢复跟随。
- 切换引擎时若 Key 输入框有未落库编辑，先提交给切换前的服务商，避免误存到新服务商名下。
- 页内完整面板关闭时 dispose 内部监听，反复打开不产生旧回调叠加。

### 交互一致性与操作直觉

- **悬停气泡补上「复制」按钮**（含「已复制」反馈）：与划词面板、输入框结果三处交互完全一致；
  加载占位时不可复制，避免把「翻译中…」存进剪贴板。
- **「原文已是目标语言，未翻译」提示**：单条翻译命中本地跳过（0 Token）时，划词面板 /
  悬停气泡 / 输入框结果都会明确说明——不再出现「译文和原文一模一样，是不是坏了」的困惑。
  数据链路贯通后台 TRANSLATE_ONE 与流式通道。
- **工具栏按钮状态感知**：页面已有译文时，悬停提示变为「收起全部译文（再次点击重新翻译）」，
  与实际的点击行为（收起 ↔ 重译）严格对应；翻译过程中逐段插入/清理实时刷新。
- **Esc 关闭悬停气泡**（含固定态）：与其他浮层的关闭语义统一；快速面板或通知弹窗
  已消费 Esc 时不再重复响应。
- **快速设置面板补齐关闭交互**：点击面板外、按 Esc 均可关闭（与完整设置面板一致）；
  齿轮变为开关语义（开着再点即关）。修复面板底边压住工具栏导致齿轮收不到点击的问题
  （上方展开现在与工具栏留出间隙）。
- **通知弹窗键盘与焦点支持**：Esc = 知道了、Enter = 主操作（如「打开设置」）、
  点击遮罩空白关闭；关闭后焦点还原到打开前的元素（兑现 aria-modal 承诺）。
- **弹窗「翻译网页」不再谎报成功**：内容脚本回传真实结果——手动模式给出切换指引、
  已暂停提示先恢复开关；工具栏空闲态文案随模式变化（手动模式明确标注「点击段落或划词即可」），
  切入手动模式时给一次性操作提示。
- 状态条加宽至 300px 并支持两行显示，长提示不再截断；时长可按文案长度调整。### 新功能

- **设置的导出 / 导入**（Options 页「备份与迁移」）：一键把引擎、语言偏好、翻译模式、
  术语表、系统提示词、站点列表导出为 JSON 文件，在其它设备导入即完成迁移。
  API Key 默认不导出（可显式勾选）；导入做严格校验与字段净化，脏数据无法进入 storage；
  无 Key 备份不会覆盖本机已填好的 Key。新增 `utils/settings-backup.ts` 与 6 项单测。
- 快速设置面板新增**翻译模式**选择（自动翻译整页 ↔ 手动点击/划词），
  与完整面板 / Popup 双向实时同步。

### 翻译核心

- 流式输出：SSE 解析顺序修正——同一块同时携带内容与 finish_reason/usage 时元数据不再丢失；
  输出被 max_tokens 截断时标记「译文可能不完整」且**不再写入缓存**（此前半截译文会污染 30 天缓存）。
- 批量协议：顶层单条目纯文本回退路径补上占位符还原，私有区（PUA）字符不再泄漏到界面与缓存。
- 故障转移单层化：消除「外层遍历 × 内层再遍历」嵌套重试，主引擎故障时最坏请求数从 O(n²) 降为线性；
  备用引擎 / 长文强模型指向「自定义 OpenAI 兼容」时正确沿用用户配置的端点与模型（此前空 baseUrl 必然失败）；
  MT→LLM 转移后质量校正发给实际成功的引擎（此前拿 MT 配置打 chat 端点必然 404 白费请求）。
- 质量自检降噪：数字等价形态归一化比较（句尾全角标点「2024。」、千分位「1000」、全角数字「５０％」、
  日期改写「2024年1月2日」、前导零），显著减少误报带来的无谓校正请求与 🚩 假警告；真缺失仍精确标记。
- 网络层超时补盲：响应体读取阶段同样受超时保护（此前服务器发完响应头停滞 body 会永久挂起）；
  SSE 流改为空闲超时并在提前终止时真正断开底层连接。
- 缓存：读命中刷新时间戳，淘汰策略从 FIFO 变为真正的 LRU 近似；长文拆分按完整码点对齐，
  emoji 不再被切成孤立代理项。
- 传统 MT 引擎批量翻译单条失败只保留原文并标记，不再拖垮整批。
- 缓存键纳入长文强模型与备用引擎配置（cacheKeyOf 不变量补齐）：强模型译文
  不再被弱引擎键命中，修改路由配置后旧缓存正确失效。
- 句子缓存路径禁用子批内部的强模型二次升级：外层按单段长度决策不走强模型时，
  多句合并请求不再因合计长度被整体送进贵模型（省 Token）。

### 页面交互

- 数字就地补丁：符号纳入 token 比对，「-5.2% → 5.2%」「50% → 50」这类纯符号变化不再被吞掉导致
  过期译文永久滞留（行情 / 天气类组件的数据正确性）。
- 取消竞态：快速连续点击「译」按钮不再产生僵尸任务或并发双整页翻译（代际计数守卫），
  同时保留「翻译中再点 = 取消，再点 = 重译」的既有语义。
- SPA 移动节点：框架 remove+insert 搬迁元素时跟随搬迁译文节点，列表排序 / 虚拟滚动不再闪烁与重复请求；
  自身译文节点移除不再引发 +8s 幽灵重译唤醒。
- 流式放弃后通知后台取消生成，同一文本不再并发生成两份（省一半 Token）；端口瞬断进入 30s 冷却自动恢复，
  不再一次失败即本页永久禁用流式。
- 无 Key 引导：悬停 / 划词 / 点段落 / 输入框四条单条路径被拦下后，填好 Key 同样自动继续翻译。
- 悬停气泡：await 期间指针已移走时不再强拆当前气泡强显旧目标。
- 固定（📌）划词面板随页面滚动平移，保持与选区原文对齐；未固定时滚动仍自动隐藏。
- click-scan 防抖窗口收集全部点击目标：快速连点不同区域时各子树都会被扫描。
- 动态重译冷却队列按元素各自到期时间调度（此前后入队者被迫跟随最早期限，
  最多晚一个完整冷却周期）；stopDynamic 完整清理冷却表与定时器。
- 工具栏位置恢复应用到当下节点：SPA 重建工具栏后位置不再丢失。
- 测试连接的成功 / 失败文案数秒后自动清除，不再永久驻留。
- 句子级缓存本地跳过时保留原文大小写（"HELLO!" 不再显示为 "hello!"）。

### 设置与 UI

- 「术语注入上限」选关闭(0)不再被 `|| 12` 兜底改写成推荐值 12。
- 选择「自定义模型…」保留已配置的自定义模型名，任意后续保存不再静默清空模型；空模型回退预设值兜底。
- 页内完整设置面板「自动翻译此站」初始状态与实际行为一致（未配置过 = 默认开）。
- 初始读取设置失败时暂停保存，防止默认值快照覆盖真实配置。
- 译文编辑粘贴一律按纯文本插入并做空白净化，杜绝富文本节点与伪术语污染个人词库。
- 拖拽监听补 pointercancel 清理（触屏手势接管后设置面板不再卡死跟随）；输入框翻译按钮阴影样式修正
  （box-shadow 属性名误写为驼峰导致从未生效）；长文阈值输入夹取下限。
- 图片翻译结果页过滤非法坐标段与 undefined 文案；弹窗空译文给出明确提示而非静默空白。
- 术语自学习写入前净化分隔符与换行，保护词库行结构；去重分支收敛为单一路径。

### 工程与测试

- 修复冒烟脚本硬编码 macOS 绝对路径与 ESLint 10 不支持的 eslint-env 注释（lint 恢复全绿）。
- 新增 15 项回归测试锁定本轮修复（流式截断不缓存、meta 同块解析、质量自检归一化、PUA 兜底、
  MT 批量降级、句子大小写保持、备份解析与字段净化、快速面板齿轮开关/外点/Esc 关闭等），
  单元测试 45 → **59**；浏览器回归 32 → **33** 全绿。

## v0.1.23 (2026-08-18)

- 语言检测：改为按段落检测语种，已是目标语言即本地跳过（0 Token），杜绝「中译中」；
  不再依赖全局源语言设置（手动把源语言设成外语时，页面中文 UI 串不再被送进模型）。
- 标识符遮罩：代码/库名/品牌专名（useState、react-dom、React、Vue、Linux 等）翻译前
  替换为占位符、译后还原，不再被翻译成中文；占位符比译文短，顺带省 completion Token。
- 首屏批量：首屏从逐条流式（N 个请求）改为单次批量请求，系统提示词/术语/上下文前缀
  不再重复发送，显著省 Token。
- 翻译模式：新增「自动整页 / 手动点击·划词」开关（设置面板）；手动模式下整页不自动
  翻译、关闭悬停与动态自动翻译，点击段落或划选文字才翻译，Token 开销最小。
- 防注入加固：页面来源的上下文（标题/前文译文）从 system 移出到 user 消息，
  页面内容不再以「指令」身份进入系统提示词；system 前缀保持稳定利于提示词缓存。
- 其它：批量指令精简（去除与注入防护重复的文案）；识别品牌专名名单；测试契约同步更新。

## v0.1.22 (2026-08-16)

- 合并远程 v0.1.21（42 个提交：流式输出 / 进度 X·Y / 注入防护 + 上下文降级 / 可拖动工具栏 / 图片翻译 / 坏 JSON 回退等），采用其更成熟的扩展源码。
- 保留本地独有贡献：整套官网 `docs/`（index / features / privacy / faq / install / providers）、`sendRuntimeMessage` 90s 兜底超时（防后台 Worker 挂起时翻译静默卡死）、术语注入上限可调（统一为远程可用的 `glossaryTermLimit`，默认 12）。
- 清理合并产生的冗余/会崩溃的死代码（重复的 `buildConfigForm` 导入、未接线的 `glossaryInjectionLimit`）。

## v0.1.21 (2026-08-15)

- 修复：大屏（完整设置）开关全部显示未开启——勾选样式同步用了 document 查询，
  Shadow DOM 内查不到（此前"大屏全白、右上角显示开"的根因）。改为在表单容器
  内查询。
- 修复：大屏与右上角设置不实时同步——update() 只重填内存旧值，不读取存储；
  改为 watch 回调携带最新配置快照刷新（外部修改即刻反映到大屏）。
- 优化：fill 仅在引擎变化时重建模型下拉（开关点击不再重建下拉，消除卡顿源）。
- 加固：表单构建异常不崩溃内容脚本（保留头部与错误提示），工具栏不受影响。
- 测试：新增开关视觉状态（is-checked）与跨上下文同步断言（31 浏览器测试全过）。

## v0.1.20 (2026-08-15)

- 优化：下拉完整显示——超长引擎名缩短（OpenRouter（Claude / GPT / Gemini …）→
  OpenRouter 等），快速面板引擎下拉加宽至 170px；所有引擎/模型/语言下拉
  增加悬停 title 提示当前完整值，不再截断。

## v0.1.19 (2026-08-15)

- 修复（重大）：完整设置面板点击即关闭——遮罩 pointerdown 用 e.target === host
  判断，但 Shadow DOM 事件重定向会把面板内点击目标重定向为 host，导致
  "点输入框/下拉就关面板"。改用 composedPath()[0] 判断真实目标，
  仅点击面板外（遮罩）才关闭。
- 测试：面板交互断言全部改为真实点击（点击输入框/下拉不关闭、选择生效、
  遮罩点击才关闭），堵住此前 fill() 不产生点击的测试盲区。

## v0.1.18 (2026-08-15)

- 修复（重大）：完整设置面板排版错乱——v0.1.14 定义 .modal 容器样式但未创建
  容器元素，head/body/foot 被遮罩 flex 拉成水平一排（内容被挤到屏幕外、
  输入框不可达、无法输入）。已补 .modal 容器包裹，面板居中凸起、圆角 20px、
  遮罩 blur(12px) 强化，布局恢复正常。
- 修复：滚轮锁定误伤面板内部——原先全局拦截 wheel/touchmove 导致面板内容
  也无法滚动；改为仅拦截面板外（遮罩上）的滚动，面板内正常滚动。
- 测试：完整设置弹窗回归扩展——居中凸起/圆角/遮罩模糊断言、输入可用断言、
  面板内滚动可用 + 页面不滚断言（31 浏览器测试全过）。

## v0.1.17 (2026-08-15)

- 修复：浏览器回归 harness 未适配 CSS 内联化——测试环境样式丢失导致布局验证失真，
  已改为复制内联样式块（真实用户构建产物不受影响）。
- 细化：打开弹窗自动聚焦翻译输入框，可直接输入即译。
- 细化：完整设置弹窗打开期间锁定页面滚动（滚轮/触摸不穿透遮罩）。
- 细化：悬停气泡「固定」按钮由 emoji 改为文字，避免部分字体缺失显示方块。
- 细化：输入框翻译按钮在矮输入框时移至框外，避免遮挡内容。
- 维护：深色布局实测复核（弹窗/设置页卡片/输入框/统计卡全部深色正确，无溢出）。

## v0.1.16 (2026-08-15)

- 稳定性：修复 configItem 初始化读取无 catch（存储不可用时未处理拒绝）。
- 稳定性：新增「连续翻译/取消/暂停/恢复」压力回归测试，验证高频操作不卡死、
  无残留译文、暂停恢复后功能完整（31 浏览器测试全过）。

## v0.1.15 (2026-08-15)

- 稳定性：完整设置弹窗的 Esc 监听随关闭移除（修复重复打开累积监听的内存泄漏）。
- 稳定性：设置同步 watch 回调全部异常隔离——任何面板刷新失败都不影响翻译主流程。
- 稳定性：设置写入改为串行队列——快速面板/大面板/自动翻译多入口并发写入不再互相覆盖。
- 稳定性：弹窗表单渲染失败时显示错误提示而非整页空白。

## v0.1.14 (2026-08-15)

- 重构：完整设置改为网页中央弹窗——全屏半透明遮罩 + 背景模糊，面板居中弹出
  带缩放动画，点击遮罩空白处或 Esc 关闭；设置自动保存。
- 同步：大面板与小面板（快速设置）双向同步——任何一边修改语言/引擎/开关，
  另一边重开后即一致；站点开关（自动翻译/暂停本站）加入大面板「本站」分区。
- 默认全开：自动翻译此站默认开启（未配置即自动翻译，用户关闭后记住），
  与"节省 Token / 智能增强"全部默认开启保持一致。
- 优化：表单外部同步不打断输入（值相同不写回控件）。
- 测试：新增大面板 modal 形态与双向同步回归（30 浏览器测试全过）。

## v0.1.13 (2026-08-15)

- 修复：快速设置面板开关点不动——input 只有 1×1 像素，点击区域几乎不存在；
  重做为 iOS 开关（input 覆盖整个开关区域），关闭灰底圆圈左、开启绿底圆圈右，
  带动画过渡。
- 重构：快速设置面板苹果风 UI——inset 分组卡片（翻译/本站/交互三组）、
  圆角选择器、统一间距；新增「悬停翻译」「输入框翻译」开关（与设置页联动）。
- 修复：面板默认定位在工具栏下方会超出视口（工具栏常驻右下角）——
  改为优先定位在齿轮上方，按实际尺寸夹取在视口内。
- 修复：完整设置面板样式改为直接打包进内容脚本（?raw），不再 fetch
  扩展资源——任何环境（含被拦截场景）都有完整样式。
- 测试：新增开关点击/持久化/iOS 视觉断言（29 浏览器测试全过）。

## v0.1.12 (2026-08-15)

- 修复：悬停/输入框翻译的设置开关未生效（模块无条件启用）。
- 修复：popup 防御样式硬编码浅色，深色系统下白字浅底看不清——颜色跟随系统。
- 修复：popup 内容高度超出浏览器 600px 上限被截断——高度封顶 + 内部滚动。
- 修复：页面内完整设置面板的样式获取与内联化构建产物不匹配（fetch 后无样式）——
  改为直接提取 options.html 内联 <style> 块，兼容两种构建形态。
- 修复：内容脚本 fetch 扩展资源缺少 web_accessible_resources 声明（会被浏览器拦截）。
- 优化：悬停气泡改为延迟隐藏，鼠标移向气泡途中不再一闪消失。
- 补全：自动翻译此站（此前 storage 已定义但功能丢失）——设置面板新增开关，
  页面加载后自动开译，与暂停列表独立。
- 测试：新增自动翻译此站与深色面板回归（28 浏览器测试全过）。

## v0.1.11 (2026-08-15)

- 修复：完整设置面板在 Edge 显示「此页面已被 Edge 阻止」——网页无法 iframe 嵌入
  扩展页面（浏览器安全策略）。改为内联渲染：从扩展读取设置页 CSS 注入面板
  shadow 后直接构建完整设置表单，功能与右上角弹窗完全一致，不再依赖 iframe。
- 修复：弹窗右侧大片空白——popup 窗口部分浏览器按 html 元素尺寸计算，
  只约束 body 宽度会留下右侧空白；html 与 body 同时固定 360px。
- 修复：CSS 内联化——构建产物页面样式直接内联 <style>，并移除 crossorigin
  与绝对路径；postbuild 改挂 WXT build:done hook，wxt zip 打包产物同样生效
  （此前 release 包未被处理，是弹窗裸渲染的根因）。

## v0.1.10 (2026-08-15)

- 修复：悬停气泡/设置面板白底看不清——宿主带 all:initial !important 时
  :host 规则会被内联样式覆盖，背景变透明导致白底白字。改为全局主题工具
  （不透明配色 + JS 直接内联，深浅色跟随系统），任何网页上都清晰可读。
- 新增：页面内完整设置大面板——悬浮设置点击「打开完整设置」即在页面内
  弹出大面板（iframe 加载扩展设置页），右上角弹窗的全部设置内容都在这里，
  可拖动、可关闭，无需跳转标签页。
- 测试：新增设置面板语言/引擎切换持久化与完整设置面板用例（26 全过）。

## v0.1.9 (2026-08-15)

- 修复：译文脉冲闪烁——流式译文到达后未移除占位脉冲动画（is-pending），
  现已在上传译文的三处更新路径中移除。
- 修复：悬浮设置面板 / 悬停气泡 / 输入框结果深色模式失效——宿主背景
  内联写死白色无法被媒体查询覆盖，改为 :host 规则随系统深浅色。
- 修复：弹窗「大片空白 + 一小列文字」——构建产物页面资源为绝对路径 +
  crossorigin，部分 Chrome 环境会拦截导致 CSS 加载失败裸渲染。
  postbuild 改为相对路径并移除 crossorigin；popup 增加防御性内联样式与
  color-scheme meta（三页面）。
- 测试：新增 CSS 多列 / 网格卡片布局对齐回归（24 浏览器测试全过）。

## v0.1.8 (2026-08-14)

- 优化：翻译进度显示——状态条实时显示「已译 X/Y 段」（Y 为本次扫描段落总数）。
- 优化：悬浮工具栏深色模式适配（底色/齿轮颜色跟随系统外观）。
- 优化：译文插入路径样式计算减负（多列检测限 3 层），性能基线最长长任务 220→192ms。
- 提示：设置页与 README 补充 Alt+T 快捷键说明；README 功能表补全新交互。

## 未发布

- 新增：悬停翻译——鼠标悬停段落 500ms 即显示译文气泡（可固定/取消），命中会话缓存零请求。
- 新增：输入框翻译——网页输入框（textarea/文本输入）聚焦时显示「译」按钮，译文浮层带复制。
- 新增：译文显示样式——默认 / 蓝色虚线 / 下划线 / 高亮块四种，设置页切换即生效。
- 新增：自动翻译此站——站点在自动翻译列表时页面加载后自动开始翻译（设置面板可管理）。
- 修复：设置页 checkbox 切换时勾选样式不同步的回归（:has 兼容替代丢失）。
- 设置页新增「悬停翻译 / 输入框翻译 / 译文样式」控件。

## v0.1.7 (2026-08-14)

- 修复：句子缓存切分误拆英文缩写（U.S. / Dr. / e.g. 等被拆成单字母），已加缩写保护。
- 修复：流式翻译失败时不再静默——错误原因会提示用户，段落自动回到懒翻译队列重试。
- 修复：非流式批量翻译后同步更新上下文窗口（上一段译文），长文一致性更好。
- 兼容：开关勾选样式移除 :has() 依赖（旧浏览器），改由 JS 同步 class。
- 优化：网页端悬浮按钮、图片翻译面板统一苹果系统蓝 + 毛玻璃 + 深色模式适配。
- 安全：术语自动学习的抽取请求增加数据边界，防止网页文本注入指令。
- 修复：译文与原文对齐（核心体验）——
  · 双栏/多列/float 布局中译文不再被挤到一栏末尾或流入下一列，译文嵌入原文块内部跟随原文；
  · 无语义块的纯文本容器（div 内多行 span / 手写 HTML / 邮件排版）按行拆分翻译，
    每行译文紧跟对应原文行，不再整块并成一段；
  · 新增双栏与纯文本块两个浏览器回归测试。
- 新增：悬浮工具栏升级为可拖动的按钮组（译 + 齿轮设置入口），位置持久化到本地，
  可拖到任意位置不遮挡视野。
- 新增：页面内快速设置悬浮窗（目标语言 / 翻译引擎 / 暂停本站 / 打开完整设置），
  可拖动、记忆位置，点击齿轮按钮即开。
- 优化：设置页宽屏（≥1280px）双栏布局，充分利用大面积。
- 修复：翻译加载态恢复后按钮宽度丢失的历史隐藏 bug（工具条意外变窄）。
- 测试：新增拖拽持久化、设置面板、双栏对齐回归用例（20 浏览器测试全过）。
- 优化：Token 用量极致压缩——
  · 系统提示词/注入防护/批量协议/风格提示全部精简（每请求省 ~60-90 token）；
  · 术语注入默认 24 条降至 12 条，按长度升序（短术语优先），总字符 500 上限；
  · 上下文窗口截断收紧（标题 80 / 前段译文 160 字符），说明文案精简；
  · 质量自检校正重试文案压缩；
  · 设置页新增「术语注入上限」控件（0/6/12/24 条），缓存键同步隔离。
- 设置页「节省 Token」区重组：句子级缓存开关归位，布局更清晰。

## v0.1.6 (2026-08-14)

- 优化：401 / 403 认证错误给出明确中文指引（区分 Key 无效/过期与账户权限问题，附服务商原始返回），不再直出英文原文。

## 未发布

- 修复：流式翻译在后台 SW 休眠/扩展重载断开端口时永久挂起（busy 卡死）——增加 25s 超时与端口断开双兜底，未完成段落自动回到懒翻译队列补译。
- 修复：传统 MT 引擎（DeepL/Google/Microsoft）不再做无意义的 LLM 校正重试。
- 优化：设置页宽屏布局加宽至 980px，宽屏下字段支持三列。

## v0.1.5 (2026-08-14)

- 新增：流式输出（SSE）——首块边生成边显示，首字延迟降至首个 token 到达（基线：92ms）。
- 新增：上下文感知翻译——注入页面标题与前段译文的滑动窗口，长文代词指代与跨段术语更连贯。
- 新增：翻译质量自检——数字/URL/邮箱/占位符/代码 token 保真校验，缺失时自动校正重试并在译文上标记提示。
- 新增：句子级缓存与归一化匹配——SPA 内容微变时只重译变化的句子，句末标点/大小写/空白差异可命中。
- 新增：多引擎路由——备用引擎故障转移（主引擎 429/5xx 自动切换）与长文强模型路由（可配置阈值）。
- 新增：译文可编辑与术语自动学习——hover 译文即可修改，LLM 自动抽取调整的术语沉淀进个人术语表。
- 优化：全应用 UI 对标苹果（iOS Human Interface）——分组卡片、系统蓝、SF 字体栈、毛玻璃、iOS 开关与分段控件、完整深色模式。
- 测试：新增质量自检/句子缓存/上下文/流式的网络层用例；新增 2000 段重页面的性能基线采集（npm run perf）。
- 文档：新增 PRIVACY.md 与三商店上架清单（docs/STORE_SUBMISSION.md）。

## v0.1.4 (2026-08-14)

- 修复：CI 依赖审计 —— `brace-expansion` 升级至 5.0.9（规避新披露的高危漏洞 GHSA-rgw5-rvv9-x895），`nanoid` / `postcss` / `esbuild` 同步升至安全版本，`npm audit` 归零。
- 修复：浏览器回归测试同步 IndexedDB 迁移 —— mock 增加最小 IndexedDB 实现，图片结果页测试改从真实数据路径验证。
- 优化：图片翻译任务存储增加 24 小时过期清理与连接释放，未打开的结果页不再长期占用存储。
- 修复：图片翻译任务改用 IndexedDB 存储，规避 `storage.local` 在 Firefox（5MB）/ Chrome（10MB）下的配额限制，大图不再写入失败或与翻译缓存争抢配额。
- 修复：Google 翻译（免 Key）明确标注为非官方体验通道；端点异常时提示更稳定、需 Key 的替代服务。
- 安全：翻译与图片请求增加 Prompt Injection 防护，将待译文本标记为数据而非指令，降低恶意网页操纵译文的风险。
- 文档：仓库已重命名为 `hao-fan`；重写 README 为「简介 / 功能 / 安装 / 快速开始 / 隐私 / 开发」结构，同步更新链接与免 Key 端点稳定性说明。

## v0.1.3 (2026-07-27)

- 品牌：统一使用新的“文/译”双向翻译 Logo，并同步扩展清单图标与所有菜单页面品牌头部。
- 优化：弹窗主页增加字符计数、成功/失败/处理中状态、策略加载锁定和可键盘操作的标签切换，统计与图片入口在窄屏下更易扫描。
- 优化：设置页读取本地配置完成前保持表单不可编辑，避免默认值闪烁或误覆盖已有设置；独立设置页统一品牌头部与移动端间距。
- 优化：图片结果页统一加载、失效、存储异常、图片加载失败和零文本状态，增加文本数量摘要、译文列表和响应式双栏/单栏布局。
- 修复：取消整页翻译后立即重新开始时，旧任务不再错误清除新任务的加载与取消状态。
- 修复：站点暂停规则启动时发生并发更新，不再被较旧的初始读取结果覆盖。
- 修复：关闭划词浮层或暂停网站会真正取消后台翻译；取消消息之前已排队的迟到请求也不会继续调用接口。
- 修复：图片引擎不受支持或配置读取失败后会重置文件选择框，同一图片可直接重试，并阻止重复上传。
- 修复：设置保存失败时不再继续测试旧配置，避免显示误导性的连接结果。
- 新增：扩展弹窗可按当前网站暂停或恢复翻译；暂停会取消任务、清理译文并阻止网页与右键图片翻译，无需刷新即可恢复。
- 测试：新增整页、划词、动态内容、批量降级、取消重启、站点规则、划词取消与图片重试的自动浏览器回归，并接入 CI。
- 安全：覆盖构建链中的高危 `brace-expansion` 传递依赖，保持高危依赖审计通过。

## v0.1.2 (2026-07-23)
- 修复：整页 / 划词翻译在 `http://` 等非安全上下文站点崩溃（`crypto.randomUUID` 替换为不依赖安全上下文的 `crypto.getRandomValues` 实现）。
- 优化：点击扫描从「整页遍历」收敛为「点击元素子树有界扫描」，避免每次点击都遍历整棵 DOM 造成卡顿，Portal 菜单与显隐切换继续由 MutationObserver 接管。
- 修复：批量译文数量偶发不一致时改为逐条回退翻译，不再因单批异常中断整页其余内容的翻译。
- 修复：批量 JSON 连续解析失败时使用并发 2 的逐条兜底，避免整批漏译或请求瞬时爆发。
- 修复：逐条兜底保留取消、错误和重试语义，不再把失败原文误记为已翻译。
- 修复：动态内容监听覆盖 `aria-expanded`、`aria-controls`、`aria-owns` 与 `data-state`，已有弹层显隐后可继续翻译。
- 修复：测试连接强制绕过缓存、术语与目标语言本地跳过，确保真正访问当前服务商。
- 修复：同步 `package-lock.json`、README 版本与实际 GitHub 发布地址。

## v0.1.1 (2026-07-23)

### 改进
- 排除站点页眉、导航、菜单、工具栏和搜索区域，避免紧凑操作区被译文撑乱；表单及对话框正文恢复翻译。
- 译文继承原文对齐方式；动态页面响应回填前校验原文，避免旧译文插入已变化的内容。
- 扩展更新或重载后不再访问失效的运行时对象，相关错误改为提示刷新当前网页。
- 页面错误改为居中的可关闭蒙版，并按单次操作去重；配置及鉴权错误不再自动重复请求。
- 动态扫描继续遍历已翻译容器的后代，并监听文本替换、异步插入及可见性切换；点击后挂载的面板和无限滚动内容可继续按视口翻译。
- 译文改为绑定原文语义块的稳定槽位，不再越过共享裁剪容器，避免倒序、错位和覆盖下一节。
- 整页文本改为分帧扫描，首批可见段落无需等待全页分析；滚动内容按视口距离提前加载。
- 翻译失败的可见段落会重新进入观察队列，最多重试两次；无限滚动不再受生命周期总数上限影响。
- 批量响应兼容对象、字符串数组和 translations 数组，异常回退最多增加两个请求，不再逐段放大 Token。
- 默认提示词压缩；缓存按 API Base URL 和协议版本隔离；截断请求计入服务商返回的 Token 用量。
- 网络请求改为 20 秒超时、最多一次重试，区分 429/5xx 并支持 Retry-After 与可取消退避。
- 自定义 OpenAI 兼容接口可手动开启图片能力；图片结果截断或格式无效时明确报错。
- CI 同时生成 Chromium 与 Firefox ZIP，版本号同步为 0.1.1。
- 修复首屏批次受字符上限切分时遗漏可见段落的问题，并清理动态页面中已移除原文对应的译文状态。
- 划词翻译在请求前锁定语义锚点；图片结果的坐标层改为与原图同一容器，避免内边距导致偏移。
- 预设服务商支持填写新模型名；自定义接口的视觉开关在弹窗上传入口同步生效。
- 发布工作流拆分只读构建与标签发布权限，并修复开发工具链中的高危传递依赖。
- 修正智谱 OpenAI 兼容端点并自动迁移旧配置；更新腾讯混元模型预设和视觉能力标记。
- API Key 改为按服务商独立保存，并自动迁移旧版单 Key 配置。
- 整页翻译支持主动取消；取消后终止网络请求并丢弃过期结果。
- 新增消息边界、配置迁移、术语表、视觉响应与请求取消单元测试。
- 启用 TypeScript 严格模式，CI 增加测试、类型检查和 Firefox 构建。
- 新增高置信度目标语言本地跳过、重复文本合并统计和 Token 用量面板。
- LLM 批量翻译改用稳定 ID 的 JSON 协议，避免分隔符错位。
- 整页翻译改为首屏小批优先、后续按字符量大批并发；检测模型截断并自动拆分超长内容。
- DeepL / Microsoft 使用原生批量接口，Google 使用受控并发，批次统计写入不再阻塞页面渲染。
- 文本收集改为语义段落模型；译文使用 Shadow DOM 隔离站点样式，并规避表格、列表和裁剪容器错位。
- 译文改为继承原文字体与颜色的轻量第二行，移除卡片背景、强调边框和阴影。
- 使用 IntersectionObserver 按视口懒翻译；未进入阅读区域的初始及动态内容不发送给模型。

## v0.1.0 (2026-07-13)

首个可用版本。

### 功能
- 沉浸式双语对照：原文下方直接插入译文。
- 网页整页翻译（右下悬浮「译」按钮 / 右键「翻译本页」），分块并发 + 逐块渲染，首屏秒出。
- 划词翻译气泡。
- **动态内容自动翻译**：翻译后自动监听页面新增节点（菜单 / 面板 / 弹窗），防抖即时翻译。
- **图片翻译**：右键图片或弹窗上传 → 视觉模型 OCR + 译文叠加显示。
- 内置 15 家引擎：OpenAI / Gemini / OpenRouter / DeepSeek / 智谱 / 腾讯混元 / 通义千问 / Kimi / 百川 / 豆包 / Ollama / 自定义 + Google / DeepL / Microsoft 翻译。
- 18 种主流语言互译，可指定源 / 目标语言。
- 本地翻译缓存（内存层 + 防抖回写 + 30 天 TTL + LRU 近似淘汰），省 Token。
- 设置页：测试连接、缓存与术语库开关。
- 苹果风浅色 UI；API Key 仅持久化在本地浏览器，并随请求直发所选服务商。

### 质量
- 新增 MIT LICENSE、.gitignore、GitHub Actions CI（打 zip 并发 Release）。
- fetch 增加 30s 超时与有限重试；4xx 不重试；处理「HTTP 200 但错误体」。
- 图片任务存储读取后即清理，避免 storage.local 堆积。
- 自定义 / 传统引擎可用文本框输入模型名。
- 显式声明内容安全策略。
